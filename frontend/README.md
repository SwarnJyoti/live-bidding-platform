# Frontend
create a file .env and add:
REACT_APP_BACKEND_URL=http://localhost:4000

Inside Terminal 1: 
cd frontend
npm install
npm start


# Backend
create a file .env and add:
PORT=4000

Inside Terminal 2:
cd backend
npm install
npm start